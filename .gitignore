# Build artifacts
hlse_core
hlse_core_static
hlse_core_cov
libhlse.so
*.o
*.so

# Test binaries (built by make)
tests/property_tests
tests/protect_tests
tests/secrets_tests
tests/supply_tests
tests/file_audit_tests
tests/util_tests
tests/corpus_ext
tests/fuzz
tests/fuzz_asan
tests/fuzz_secrets
tests/fuzz_secrets_asan
tests/fuzz_supply
tests/fuzz_supply_asan
tests/fuzz_file
tests/fuzz_file_asan
tests/fuzz_url
tests/fuzz_url_asan
tests/fuzz_server
tests/fuzz_server_asan

# Coverage
*.gcov
*.gcda
*.gcno

# Python (examples/python_integration.py byte-cache)
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# API server binary
hlse-server
tests/server_tests

# Release artifacts / prebuilt binaries (never commit these — see the
# "files (1).zip" incident: a stale v0.5.0 build artifact was committed
# twice because nothing blocked it)
*.zip
*.tar.gz
*.tar.bz2
*.dSYM/
